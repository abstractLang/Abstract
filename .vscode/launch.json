{
    "version": "0.2.0",
    "configurations": [

        {
            "name": "F5 Anything: Custom Command",
            "type": "f5anything",
            "request": "launch",
            "command": "clear && zig build run -freference-trace -- compile ./test-project"
        }
    ]
}
