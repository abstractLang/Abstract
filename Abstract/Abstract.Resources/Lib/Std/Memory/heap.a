@public struct WasmAllocator
{

	func alloc(uptr length, Alignment alignment) Memory {
        let Memory ret = new Memory()
        ret.length = 10
        return ret
    }
    func resize(Memory old, uptr newLength) bool {
        return false
    }
    func realloc(Memory old, uptr newLength) Memory {
        let Memory ret = new Memory()
        return ret
    }
    func free(Memory memory) void {
        return
    }

}
