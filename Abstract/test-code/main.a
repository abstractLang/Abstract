@public func main() void {

	let Randomizer myRandomizer
	foo(5, myRandomizer)

}

func foo(u8 entropy, Randomizer rng) void {

	let i32 value = 42
	let i32 smtgidk = 500
	
	value *= 5 + smtgidk * entropy

}

struct Randomizer {

}
